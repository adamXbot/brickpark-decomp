# Scope PORT-M12 — the Space Tower's "two halves", and the ten +0xa0 draw handlers M11 could not see

> **PORT-M12 — Status: IN PROGRESS (claimed 2026-09-12 by PORT-M12)** — branch
> `scope/PORT-M12`. Matching side: `LEGOLAND/*.c` under
> `#ifdef LEGOLAND_PORTABLE`, VC6-gated per file, with read access to the shim.
> Brief: the browser build's rendering defect on a placed ride (PORT-B10's
> PARK-3). Notes: this file.

Findings and measurements land here as the lane runs; the verdict, the gate
table and the before/after crops are at the end.
