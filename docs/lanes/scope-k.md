# scope K — movie player, path masks, textures (claimed 2026-09-05)

Claimed by a Fable 5.1 session (checkout `~/Downloads/legoland/legoland`) at
2026-09-05 19:40 AEST, branch `scope/K` off `f8f5854`. Three Opus lanes, one
per file (`movie.c`, `pathmask.c`, `texture.c`). This stub exists so a second
session sees the claim before starting the same scope (scope E was done twice
today). It is replaced by the folded lane notes when the branch is finished.
