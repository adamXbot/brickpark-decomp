# Scope PORT-M8 — the toolbar click, the `+0xb0` mixed slot, and twelve declarations

> **PORT-M8 — Status: IN PROGRESS (claimed 2026-09-12 by PORT-M8)**
> Branch `scope/PORT-M8` from `feat/decomp-completion-next-steps-24a0d6` @
> `825a90b5` (the PORT-M7 merge). Files owned: `LEGOLAND/*.c` as needed plus an
> append-only block in `portable/tests/test_callback_types.c`. `portable/**`
> otherwise and `docs/HANDOFF.md` belong to other lanes.

The work: PORT-A7 §7 A7-2 (a park toolbar click is a `call_indirect` type
mismatch), PORT-M7 §2's open `+0xb0` mixed body set, PORT-A7 §6's twelve
declaration findings, PORT-M7 §8.5's stale `bigscreens.c:57` icon-input
spelling, and PORT-M6 §1f's multi-address extern sweep re-run tree-wide.

Notes are filled in per item below as each is closed.
